package Classes;

/**
 * An enum that holds the type of upgrade. This is used to determine how the toolbar should be upgraded.
 * @author 0xc60f
 */
public enum UpgradeType {
    RING, ARCHIVE, RESEARCH
}
